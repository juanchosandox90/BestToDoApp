<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BestToDoApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/darkerPurple</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/darkerPurple</item>
        <item name="colorSecondaryVariant">@color/darkerPurple</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.BestToDo.Multiline" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/custom_input_light</item>
        <item name="android:textSize">14sp</item>
        <item name="strokeWidth">1dp</item>
        <item name="hintTextColor">@color/darkerPurple</item>
        <item name="strokeColor">@color/darkerPurple</item>
        <item name="android:textColor">@color/lightGrey</item>
        <item name="android:textColorHint">@color/darkerPurple</item>
        <item name="android:radius">6dp</item>
    </style>

    <style name="CustomOutlineBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/text_input_layout_stroke_color</item>
        <item name="android:textColorHint">@color/text_color_hint</item>
        <item name="hintTextColor">@color/darkerPurple</item>
    </style>

    <style name="CustomConstraintItem" parent="Theme.AppCompat.DayNight">
        <item name="android:background">@drawable/item_background</item>
    </style>
</resources>